#ifndef CLASSIFIER
#define CLASSIFIER
namespace enums
{
    enum Classifier
    {
        resnet = 0,
        densenet = 1,
        vgg = 2,
        googlenet = 3,
        efficientnet = 4
    };
}

#endif